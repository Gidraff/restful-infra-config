{
    "variables": {
        "aws_access_key": "{{env `AWS_ACCESS_KEY`}}",
        "aws_secret_key": "{{env `AWS_SECRET_KEY`}}"
    },

    "builders": [{
        "type": "amazon-ebs",
        "access_key": "{{user `aws_access_key`}}",
        "secret_key": "{{user `aws_secret_key`}}",
        "region": "us-east-2",
        "source_ami": "ami-916f59f4",
        "instance_type": "t2.micro",
        "ssh_username": "ubuntu",
        "ami_name": "nat-instance {{timestamp}}"
    }],

    "provisioners": [
        {
            "type": "shell",
            "script": "../scripts/setup_python.sh"

        },
        {
            "type": "ansible",
            "user": "root",
            "playbook_file": "../playbooks/playbook_nat.yml"
        }
    ]

}
